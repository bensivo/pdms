Create a reference-list field
    G: I have 2 entities (e.g., Task and Team Member)
    W: I go to configure Task, add a field, and choose the 'reference-list' type
    T: A list of all other entities appears, I can select Team Member

Populate a reference-list field when creating
    G: I have an entity with a reference-list field
    W: I go to create a record of that entity
    T: The reference-list field shows a multi-select dropdown with all available records
    T: I can select multiple records at once
    T: Selected items appear with X buttons to remove them

View reference-list in list view
    G: I have a record with a reference-list field containing multiple selected records
    W: I view that field in the 'All X' page
    T: Each selected record name appears as a separate clickable link
    T: Clicking a link navigates to that record's detail page
    T: If the field is empty, it shows a dash (—)

Edit reference-list field
    G: I have a record with a reference-list field that already has multiple selections
    W: I go to that record's detail page and click 'Edit'
    T: The reference-list field shows a multi-select with all previously selected items pre-populated
    T: I can add or remove items from the selection
    T: After saving, the changes are persisted

Reference-list with no selections
    G: I have a record with an empty reference-list field
    W: I view it in the All X page
    T: The field displays a dash (—)
    W: I go to the detail page and click Edit
    T: The multi-select is empty

Display name works with reference-list
    G: I have Task and Team Member entities where Team Member has a displayNameField set to "Name"
    W: I create a Task with multiple Team Members selected in reference-list
    T: The list view and detail page show the display names, not the record IDs

Reference-list backlinks
    G: I have Team Member entity with a backlink field pointing to Task's reference-list "Assignees"
    W: I view a Team Member record
    T: The backlink shows all Tasks where that Team Member is in the Assignees list

Bulk create with reference-list
    G: I have Task entity with reference-list field
    W: I go to Tasks list and click "Bulk Create"
    W: I select reference-list field to include
    T: The spreadsheet shows a multi-select for the reference-list column
    W: I select multiple items for multiple rows
    T: After creating, all records have correct reference-list values
