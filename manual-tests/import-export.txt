Export workspace data
    G: A workspace with entities, fields, and records
    W: I go to Workspace Config and click "Export Data"
    T: A JSON file downloads with filename pattern "pdms-export-YYYY-MM-DD.json"
    T: The file contains version, exportedAt, entities array, and records array
    T: All my entities and their configurations are in the file
    T: All my records and their data are in the file

Import data to empty workspace
    G: A fresh/empty workspace
    G: I have a previously exported JSON file
    W: I go to Workspace Config and click "Import Data"
    W: I select the exported JSON file
    T: A success message appears: "Data imported successfully!"
    T: All entities from the export appear in the entities list
    T: All records from the export appear in the entity list views

Import overwrites existing data
    G: A workspace with existing entities and records
    G: I have a JSON export from a different workspace
    W: I go to Workspace Config and click "Import Data"
    W: I select the JSON file
    T: A success message appears
    T: All old entities are replaced with ones from the import
    T: All old records are replaced with ones from the import

Import with reference fields
    G: A JSON export containing entities with reference fields and linked records
    W: I import the JSON into a fresh workspace
    T: All reference fields are recreated
    T: All records with references point to the correct related records
    T: Reference links work and navigate to the correct record

Import with reference-list fields
    G: A JSON export containing records with reference-list fields (multiple selections)
    W: I import the JSON into a fresh workspace
    T: All reference-list fields are recreated
    T: All records show the same multiple selections as before export
    W: I edit a record with reference-list
    T: The multi-select shows all previously selected items

Import invalid file
    G: A corrupted or invalid JSON file
    W: I click "Import Data" and select the invalid file
    T: An error message appears explaining the problem
    T: My workspace data remains unchanged

Import empty workspace
    G: A JSON export from an empty workspace (no entities, no records)
    W: I import it into a workspace with existing data
    T: My workspace becomes empty after import
    T: No entities or records remain

Large dataset export/import
    G: A workspace with 50+ records across multiple entities
    W: I export the data
    T: The file downloads successfully with all records
    W: I import it into a fresh workspace
    T: All records are imported correctly
    T: List views show correct record counts

Export file is valid JSON
    G: An exported JSON file
    W: I open it in a text editor
    T: The JSON is properly formatted and readable
    T: It can be parsed by a JSON validator without errors

Delete record and re-import
    G: I have imported data, then deleted a record
    W: I export again, then clear the workspace
    W: I import the first export (which still has the deleted record)
    T: The deleted record reappears after import
